function setup() {
    createCanvas(windowWidth-4, windowHeight-4); // makes the canvas (between header and footer)
    frameRate(60);
    strokeWeight(1); // thickness of outline
}

function draw() {
    colorMode(HSB);
    background(300,6,96,10);
    for (x=100; x<=width-100; x++) {
        push();

        pop();
    }
}